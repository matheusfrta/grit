[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "grit-toolkit"
version = "0.1.1"
description = "resilience toolkit: retry, circuit breaker, timeout, rate limit, bulkhead, fallback"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["grit*"]

[tool.pytest.ini_options]
testpaths = ["tests"]